"use strict";
/*
import { openMemo } from "../../main";

var should = require("should");

export function test_openMemo() {
    describe("openMemo()", function() {
        
        it("should create a memo", function() {
            const { remember, recall, forget } = openMemo();
            should(remember).not.undefined();
            should(recall).not.undefined();
            should(forget).not.undefined();
        });

        describe(".remember()", function() {
            const { remember, recall } = openMemo();

            it("should register a value", function() {
                remember("name", "mona");
                should(recall("name")).equal("mona");
            });
        });

        describe(".recall()", function() {
            const { remember, recall } = openMemo();

            remember("name", "mona");

            it("should retrieve a value", function() {
                should(recall("name")).equal("mona");
            });
        });

        describe(".forget()", function() {
            const { remember, recall, forget } = openMemo();

            remember("name", "mona");

            it("should remove a value", function() {
                forget("name");
                should(recall("name")).undefined();
            });
        });
    });
};
*/ 
